# LogBook — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git — it is already in .gitignore.

# ─── Supabase ────────────────────────────────────────────────
# Found in: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...

# ─── Supabase Service Role (server-side only, NEVER in frontend)
# Found in: Supabase Dashboard → Project Settings → API → service_role key
# Used by api/chat.js and api/webhook.js to verify Pro status
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ─── Anthropic (server-side only, NEVER in frontend) ─────────
# Found in: console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-...

# ─── Stripe (server-side only, NEVER in frontend) ────────────
# Found in: dashboard.stripe.com → Developers → API Keys
STRIPE_SECRET_KEY=sk_live_...

# The Price ID of your Pro subscription product.
# Create it in: Stripe Dashboard → Products → Add Product
# → Add a recurring monthly price → copy the price ID (price_xxx)
STRIPE_PRICE_ID=price_...

# Webhook secret for verifying Stripe events.
# Create it in: Stripe Dashboard → Developers → Webhooks
# → Add endpoint → your-vercel-url.vercel.app/api/webhook
# → Select events: checkout.session.completed, customer.subscription.deleted
# → Copy the signing secret
STRIPE_WEBHOOK_SECRET=whsec_...
